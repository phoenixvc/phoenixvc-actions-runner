# Terraform
.terraform/
!.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Secrets — never commit keys or credentials
*.pem
*.key
*.b64
terraform/key.*
